# Frontend consolidation (see docs/BACKLOG.md): all web traffic goes to the
# Vue PWA — it is installable, responsive, and websocket-live, so maintaining
# a UA-routed Flutter web build doubled every frontend change for no gain.
# To re-enable the Flutter PWA for mobile UAs, restore the ~*android/~*iphone/
# etc. entries pointing at http://flutter-pwa:80.
map $http_user_agent $mobile_upstream {
    default "http://frontend:80";
}

# Redirect HTTP to HTTPS on port 19991
server {
    listen 80;
    server_name _;
    
    # Allow Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Redirect all HTTP traffic to HTTPS on port 19991
    location / {
        return 301 https://$host:19991$request_uri;
    }
}

# HTTPS server on port 19991
server {
    listen 19991 ssl http2;
    server_name _;
    
    # Docker internal DNS resolver (required when using variables in proxy_pass)
    resolver 127.0.0.11 valid=30s;
    
    # SSL certificate paths (mounted from host)
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    
    # SSL configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Proxy WebSocket connections to backend
    location /ws {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Port 19991;
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }

    # Proxy Django admin to backend
    location /admin {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Port 19991;
        proxy_set_header X-Forwarded-Host $host;
    }

    # Serve Django media files from backend
    location /media {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Port 19991;
    }

    # Proxy API requests to backend
    location /api {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Port 19991;
        proxy_cache_bypass $http_upgrade;
    }

    # Route based on user agent (mobile -> Flutter PWA, desktop -> Vue frontend)
    location / {
        proxy_pass $mobile_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Port 19991;
        proxy_set_header X-Forwarded-Host $host;
    }
}

